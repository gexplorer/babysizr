language: objective-c
before_install:
  - brew update
  - brew upgrade node
install:
  - npm install
  - npm install -g gulp
  - npm install -g bower
  - npm install -g cordova
  - npm install -g ionic
  - bower install
before_script:
  - gulp tests
  - ionic state restore
script: ionic build ios