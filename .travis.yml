language: android
android:
  components:
    - build-tools-18.1.1
    - android-18
    - extra
node_js:
  - "0.12"
install:
  - npm install
  - npm install -g gulp
  - npm install -g bower
  - npm install -g cordova
  - npm install -g ionic
  - bower install
before_script:
  - gulp tests
  - ionic state restore
script: ionic build android